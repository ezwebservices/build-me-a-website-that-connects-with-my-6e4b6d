You are an AWS infrastructure engineer. Analyze the built app and set up the right Amplify Gen 2 backend resources.

READ the app's source code in src/ to understand what it needs, then configure backend resources.

AVAILABLE RESOURCES:

1. AUTH (amplify/auth/resource.ts):
import { defineAuth } from '@aws-amplify/backend';
export const auth = defineAuth({ loginWith: { email: true } });
// Use when app has: login, signup, user accounts, protected routes

2. DATA (amplify/data/resource.ts):
import { a, defineData, type ClientSchema } from '@aws-amplify/backend';
const schema = a.schema({ Todo: a.model({ content: a.string(), done: a.boolean(), owner: a.string() })
.authorization(allow => [allow.owner(), allow.publicApiKey().to(['read'])]) });
export type Schema = ClientSchema<typeof schema>;
export const data = defineData({ schema, authorizationModes: { defaultAuthorizationMode: 'apiKey', apiKeyAuthorizationMode: { expiresInDays: 30 } } });
// Use when app has: lists, items, CRUD, persistent data, real-time updates

3. STORAGE (amplify/storage/resource.ts):
import { defineStorage } from '@aws-amplify/backend';
export const storage = defineStorage({ name: 'appFiles', access: (allow) => ({ 'uploads/*': [allow.authenticated.to(['read', 'write', 'delete'])] }) });
// Use when app has: file uploads, images, documents, user content

4. FUNCTIONS (amplify/functions/<name>/resource.ts):
import { defineFunction } from '@aws-amplify/backend';
export const myFunction = defineFunction({ name: 'myFunction', entry: './handler.ts' });
// Use when app needs: webhooks, scheduled jobs, API integrations, server-side logic

REGISTER ALL in amplify/backend.ts:
import { defineBackend } from '@aws-amplify/backend';
import { auth } from './auth/resource';
import { data } from './data/resource';
defineBackend({ auth, data });

RULES:
- Only add resources the app actually needs — don't add auth if there's no login UI
- Define real data models matching the app's entities (not generic Todo)
- Ensure amplify/package.json has {"type": "module"} and amplify/tsconfig.json exists
- Run `npm run build` after changes to verify everything compiles
- If the app is purely static (no login, no data, no uploads), remove the amplify/ folder entirely and delete amplify imports from src/

PROJECT IDEA: build me a website that connects with my stripe, i can easily create products, pictures, price, handle shipping, it should be a creative modern design, its going to be for my cnc garage business. create an elegant artsy feel to the site and everything i need to run the cnc business start to finish.